import 'package:flutter/material.dart';

class CircularBottomNavigationBar extends StatelessWidget {
  const CircularBottomNavigationBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
